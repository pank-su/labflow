# Artifact contract

Keep this file internal. Fill from current sources; unknown values stay unknown.
A bounded revision updates the existing contract instead of reinitializing a project.

## Scope and deliverables

- Scope: full / bounded revision / publication
- Requested artifacts, exact output paths, and audience:
- Applicable phases; inapplicable phases with reasons:
- Current input/artifact identity (path and SHA-256):
- For revisions: changed content, unaffected content, affected dependencies/pages:
- Publication/submission authorization and target (separate from local delivery):

## Source and style decisions

| Decision / requirement ID | Current source and locator | Chosen rule | Conflict or accepted exception |
|---|---|---|---|

Include the authoritative template path/version/hash, title-page mode, section
order, table/figure conventions and font sizes only where the source prescribes
them. A previous artifact supplies style, not automatically current content.

## Calculation presentation

- Source notation and permitted newly defined symbols:
- Units and representations (e.g. dimensionless fraction versus percentage):
- Internal precision and displayed rounding, separately:
- Worked examples required for each distinct formula/stage:
- Interpretation expected in the conclusion:
- Internal limitations versus material limitations required in the report:

## Review and delivery

- Requirement IDs and matching evidence/check commands:
- Required page-level visual coverage:
- Candidate inputs included in fingerprint; review-owned output exclusions:
- Per-item state for batches; missing sources and next retry conditions:
- Required independent reviews; stop/retry boundary:
- Actual delivered file paths, never an older similarly named artifact:

Do not fill acceptance fields with guessed facts or a preselected passed status.
